ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            title:
              blank: が空です。
              too_long: が%{count}文字が超えています。
            content:
              too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]
            base:
              content_attachment_is_too_big: 添付ファイルは%{max_mega_bite_size}MBサイズ以下にしてください。[%{byte_size}/%{max_content_attachment_byte_size}]
    attributes:
      post:
        title: タイトル
        content: コンテンツ

